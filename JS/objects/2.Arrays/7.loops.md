**Arrays: non-return**

**for…of**

```js
for (var item of myArray) {
  console.log(item); // logs items in myArray
}
```

**forEach()**

```
myArray.forEach(item) {
  console.log(item); // logs items in myArray
});
```