var original = [6,4,0, 3,-2,1]
var names = ['abe', 'bert', 'carl', 'dave', 'el', 'fort']

var obj = [];
for (i=0; i<original.length; i++) {
  obj.push({name: names[i], age: original[i]})
}
console.log(obj)
// for (first = 0; first<obj.length-1; first++) {
//   for (sec = first+1; sec< obj.length; sec++){

//   }
// }

 